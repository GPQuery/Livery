'use strict';

var path  = require('path');
var gulp  = require('gulp');
var conf  = require('./conf');

var $ = require('gulp-load-plugins')({
  pattern: ['gulp-*', 'uglify-save-license', 'del']
});

var paths = {};

gulp.task('svgSprite', functino() {
  //return gulp.src(path.join(conf.src))
})
